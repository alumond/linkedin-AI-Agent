# Codex image task

The local Codex task **LinkedIn Codex images** (`linkedin-codex-images`) was activated on **26 September 2026** for **08:00 Africa/Lagos, Monday–Friday**. It continues in the LinkedIn Studio task. The owner explicitly approved recurring draft preparation, Codex image generation/review and pushes of only each image and its review record. Keep the device awake and Codex running for generation. Publication is separately scheduled for 09:17 and always requires the owner's approval in the local dashboard or Mac app. The first scheduled execution has not yet been verified.

## Task prompt

Work in `/Users/admin/Downloads/LinkedinAgent` on Almond's LinkedIn Studio. Read README.md, config/agent.yaml and src/linkedin_ai_agent/codex_visuals.py. Use the imagegen skill and built-in Codex image generation, never Gemini images or Python-rendered image substitutes. Preserve unrelated local work. Do not approve posts on the owner's behalf or dispatch a live publication.

1. Use `gh` or `.tools/gh_2.94.0_macOS_arm64/bin/gh` for `alumond/linkedin-AI-Agent`. Check that no weekday publisher run is active. Dispatch `weekday-linkedin-post.yml` with `mode=prepare` and `dry_run=true`; wait for completion and inspect the actual result. This alternates fresh public GitHub evidence with grounded data, AI, analytics and industry research, or preserves the exact pending draft. It also processes an owner's requested revision. A failed idea should lead to another verified project/feature, never recycled copy. Stanforteedge and all HR dashboard/HR analytics material are excluded organisation work.
2. Download that run's `linkedin-agent-reports` artifact into an isolated temporary directory. Read `.state/pending_image_post.json`, publication history, reports and the matching `assets/briefs/*.json`. Never overwrite local state with an unverified or older snapshot. Confirm the pending draft still matches the `automation-state` branch before delivering assets. If a valid reviewed image already exists for this exact draft, preserve it and report that the post is ready for owner review; do not regenerate approved work.
3. Read the complete post, evidence and any owner feedback. Generate a new useful image for this exact draft. Match the configured visual direction. Workflow explainers are part of the editorial mix: for these posts, illustrate the workflow being explained (for example, data to report, an AI-assisted analysis process, or risk escalation). Show its supported stages, decisions, people/tool handoffs, revision paths and output using a process map, decision flow or swimlanes. Check arrow directions and distinguish proposed steps from implemented behavior. Do not substitute the LinkedIn publishing workflow unless that is the actual post topic. No sketches, model drawings, wireframes, stock people, fake screenshots, generic AI artwork, internal drafting instructions, invented statistics, tiny type or cropped titles. Compare the recorded earlier images and avoid their composition, not just their filenames or colours. Choose a visual form appropriate to the actual feature; do not repeat a single infographic layout every day.
4. Inspect the actual generated image. Check spelling, phone-size readability, composition, source alignment and truthfulness. If unsuitable, name the defect and regenerate for the same post, up to three attempts. Preserve the draft and specific failure details if generation fails. Never attest a review that did not happen or reuse a legacy image.
5. Copy the passing image to the exact asset path in the brief. Create its sibling JSON review record with the fields in README.md, actual prompt, draft and asset SHA-256 fingerprints, accurate alt text, review time and meaningful review notes. The record describes Codex's visual inspection; the user's separate approval is still required.
6. Verify using `reviewed_visual` plus an isolated agent dry run with the downloaded current history, reports and pending draft. Check image hash/perceptual similarity and text originality. Never call LinkedIn publishing endpoints. Do not change text to fit an unrelated image.
7. Commit and push ONLY the new image and its review record to the publisher's current main branch with a normal non-force push. Check the current remote pending draft again before committing. Verify the remote asset bytes and review fingerprints. A local-only file is not delivered.
8. Dispatch `weekday-linkedin-post.yml` with `mode=publish` and `dry_run=true` to verify the deployed handoff. Inspect its result and report the exact draft, image, generation attempts and verification outcome. Open or link the review desk at `http://127.0.0.1:8765`. Tell the owner the post is ready to review. Never write `.state/approved_post.json`; only the owner may approve using the dashboard.

## Activation check

The automation tool confirmed status `ACTIVE`. Activation and a successful scheduled execution are separate checks: do not claim a scheduled run succeeded until its actual result is verified. A missed approval or unavailable image leaves the post available for the next schedule; it does not authorize publishing a substitute.
